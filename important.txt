vector:
     push_back();
     find();     //#include<algorithm>
     reverse();
     clear();
     sort();   //#include<algorithm>
     erase();
     
     
list:
     push_front();
     push_back();
     pop_front();
     pop_back();
     find();     //#include<algorithm>
     reverse();
     clear();
     sort();   //#include<algorithm>
     erase();
     insert();
     
